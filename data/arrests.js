// arrests.js - arrests data layer placeholder

const exportedMethods = {
  async getArrestById(id) {
    return { id };
  },

  async searchArrests(query, filters, pagination) {
    return [];
  }
};

export default exportedMethods;
